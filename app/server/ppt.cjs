// ppt.cjs (stub) — 원본의 PPT 제작(Notion+Google Slides 연동) 기능은 OpenRM 코어에서 제외.
'use strict'
async function generate() {
	return { ok: false, error: 'disabled in OpenRM core' }
}

module.exports = { generate }
